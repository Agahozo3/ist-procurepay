---

# 🎨 Frontend (React) Setup

The frontend of this project is built using **React**, with API communication handled through Axios.  
It includes full authentication flow, dashboards for different user roles, and integration with the Django backend.

---

## ⚛️ Tech Stack (Frontend)
- **React (Vite or CRA)**
- **React Router**
- **Axios**
- **Tailwind CSS**
- **Lucide Icons**
- **LocalStorage Authentication**
- **Context API (optional)**

---

## 📂 Frontend Project Structure

frontend/
│── src/
│ ├── api/ # Axios instances & API calls
│ ├── pages/ # Page components (Login, Dashboard, Requests, etc.)
│ ├── components/ # Reusable components
│ ├── App.jsx # Routing setup
│ └── main.jsx # Entry point
│── package.json
│── README.md


## 📦 Install Dependencies

Navigate into your frontend folder:

```bash
cd frontend
npm install
